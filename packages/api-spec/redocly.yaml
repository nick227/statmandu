extends:
  - recommended

rules:
  info-license: off
  no-server-example.com: off
  tag-description: off
  operation-4xx-response: off
  # Shared components (Cursor, Limit, PaginatedMeta, etc.) are declared once
  # up front and adopted domain-by-domain across iterations — expected to be
  # temporarily unused between iterations.
  no-unused-components: off
