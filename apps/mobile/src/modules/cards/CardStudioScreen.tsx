import { CardBuilderScreen } from './builder/CardBuilderScreen'

export function CardStudioScreen() {
  // Canonical builder UI is now the step-based Card Builder experience,
  // backed by real SDK Card endpoints (create/update/generate/publish).
  return <CardBuilderScreen />
}
